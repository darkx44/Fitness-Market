# Created by venv; see https://docs.python.org/3/library/venv.html
venv/
__pycache__/
*.pyc
.env
.DS_Store

# ML / data
data/
# models/ est suivi volontairement : le modèle est petit (~0,5 Mo) et doit être
# présent sur GitHub pour que l'hébergeur (Render) puisse construire l'image.
# Pour de GROS modèles, on utiliserait plutôt un stockage externe (S3, etc.).

# notebooks cache
.ipynb_checkpoints/